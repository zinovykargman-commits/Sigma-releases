# Sigma releases

Public Android update channel for Neon District.

- `latest.json` tells the installed app which version is current.
- `NeonDistrict.apk` is the current signed installer downloaded by the app.
- The Godot source remains in the separate private `sigma` repository.

Android will always ask the user to approve installation of an APK update.
